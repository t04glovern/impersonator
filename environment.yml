name: impersonator
channels: # Allow conda-forge (community packages)
  - omgarcia
  - defaults
dependencies:
  - python=3.6
  - jupyter
  - gcc-6
  - matplotlib
  - numpy
  - pip
  - pip:
    - imageio
    - tqdm
    - setuptools
    - scipy
    - torch==1.2.0
    - torchvision==0.4.0
    - h5py
    - scikit_image
    - visdom
    - opencv_contrib_python
    - ipdb
    - Pillow
    - progressbar2
    - tensorboardX
